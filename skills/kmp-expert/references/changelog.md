# Changelog

Part of `kmp-expert`. Load this file when working on: changelog.

---

| Date | Change |
|---|---|
| 2026-08-24 | Added `kmp-kotlinpoet` (74th skill) — user asked whether this collection uses KotlinPoet; confirmed no, and that the one real Kotlin-codegen tool (`kmp-imagevector-generator`) is a Python script where a JVM library like KotlinPoet couldn't apply at all. Real gap: no skill teaches authoring a custom KSP processor, the actual scenario KotlinPoet exists for. Verified the real API (FileSpec/TypeSpec/FunSpec/PropertySpec, format specifiers), the two-module processor structure and `META-INF/services` ServiceLoader registration requirement, and the `kotlinpoet-ksp` interop module's real conversion functions before writing. Worked example generates a Koin module, consistent with `kmp-dependency-injection`. Layer 2 — Core Infrastructure, no deps. |
| 2026-08-24 | Added `kmp-security` (73rd skill) — user asked whether code security and obfuscation were covered; confirmed obfuscation existed (`kmp-proguard-r8`, Android-only) but certificate pinning, root/jailbreak/tamper detection, secure storage as an owned topic, and iOS/Native binary stripping were all real, zero-coverage gaps. Verified every library before writing: Ktor has no native cross-platform pinning (real `expect`/`actual` requirement), freeRASP ships a real dedicated KMP variant, KSafe is a real KMP encrypted-storage library, OWASP Mobile Top 10's current version is the real 2024 release. Includes a coverage map cross-referencing where each OWASP category is actually handled across this collection. Layer 1 — Project Foundation, cross-referenced from `kmp-proguard-r8`. |
| 2026-08-24 | Added `kmp-resilience` (72nd skill) — real, evidence-backed gap: ts-agent-skills already had a full `ts-resilience` skill, kmp-agent-skills had nothing equivalent, confirmed while investigating a real KMP native-binding project's asymmetric backend resilience (one platform implementation recovered from a transient resource-loss error, its sibling didn't). Layer 2 — Core Infrastructure, "depends on: network-layer" in the Dependency Graph, added to Build Order Phase 1 step 5 (not excluded like `kmp-mcp-sdk`/`kmp-audit` — resilience is foundational enough to belong in the linear build order, matching `ts-resilience`'s own placement in `ts-expert`'s Build Order). |
| 2026-08-23 | Added `kmp-mcp-sdk` (71st skill) — user asked whether the collection covers `modelcontextprotocol/kotlin-sdk`; confirmed a real, zero-coverage gap. Verified real and official (maintained with JetBrains, KMP-native across JVM/Native/JS/Wasm) via `gh api` before writing. Layer 2 — Core Infrastructure, alongside `kmp-network-layer`/`kmp-kotlin-rpc`; "no deps" in the Dependency Graph, same treatment as `kmp-ktor-auth-service`/`kmp-mongodb-database`. |
| 2026-08-19 | Added `kmp-coroutines-flow-patterns` (70th skill) — real gap found comparing coverage against a third-party skill collection: no skill covered general coroutine/Flow patterns as their own topic, scattered implicitly across `kmp-mvi`/`kmp-repository-pattern`. Added to Layer 1, Skill Invocation Map, and `agents/planner.md`'s routing table. Splitting the Skill Invocation Map out to `references/skill-invocation-map.md` (SKILL.md hit 502 lines) broke both `validate_skill_map.py` and `validate_keyword_routing.py`, which previously read the map's text directly off SKILL.md and had no notion of following a `references/` pointer for it — both now do. Also caught and fixed a real, separate staleness bug found in the same pass: `agents/planner.md` said "Our 66 skills" while the repo had 69 (three releases stale) because the count-phrase check only ever covered README.md and `agentskills-io-standards.md`, never `planner.md` — extended it. |
| 2026-08-07 | Added `references/new-project-phase-1..5-*.md` — the five phases of `/kmp-new-project`, moved out of the command (1390 → 64 lines, closing KI-009). Template extraction alone couldn't do it: only 31% of that command was fenced content, so removing every template still left ~794 lines of genuine ordered procedure. Split by phase instead, each self-contained with an explicit gate before the next; the command is now the index. Also fixed a second drift bug found on the way — F-01 carried its own copy of kmp-wizard's module map that disagreed with `kmp-feature-scaffold`'s about when `:server` exists (that pair had already drifted once before on `:androidApp` vs `:app:androidApp`); it now delegates to `kmp-feature-scaffold`'s `references/step3-clone-kmp-wizard.md` as the single owner. |
| 2026-08-04 | Added `references/agents-md-templates.md` — the literal `.claude/AGENTS.md` (APP + LIBRARY) and `CLAUDE.md` bodies `/kmp-setup-agents` writes, moved out of that command so it clears the 500-line guideline (638 → 496, KI-009). Deliberately a **skill** reference rather than a plugin-root `assets/` file: `/kmp-setup-agents` is consumer-facing, and `update-consumer-skills.sh --install-commands` copies a command as a single bare `.md` — skills are always deployed, assets aren't, so an `assets/` path would resolve here and be missing in every consumer project. This is also now the single owner of those templates after `/kmp-new-project`'s duplicate copy was found to have drifted (its LIBRARY variant had lost five skill-routing rows). |
| 2026-08-04 | Split SKILL.md (919 lines) into 8 `references/*.md` files (Dependency Graph, Build Order, Feature Slice Checklist, Decision Trees, Quick Health Check, Required vs Optional Skills, Project-Specific Source of Truth, this Changelog), leaving pointer stubs plus a new References section. Kept "The 68 Skills and What They Own" and "Skill Invocation Map" inline — `validate_skill_map.py`/`validate_keyword_routing.py` check this file's own text directly and don't read `references/`. SKILL.md drops to 490 lines. Fixed `tests/test_docs_governance.py::test_claude_scaffold_contract_is_documented_as_project_owned_plus_runtime`, which asserted `"rules/"` etc. against `kmp-expert/SKILL.md` alone — the content it checked moved into `references/project-specific-source-of-truth.md`; added a `read_skill_with_references()` helper mirroring the same fix already applied to `check_compat_matrix.py`. Part of the same backlog cleanup as `kmp-compose-design-system`/`-extended`/`kmp-mvi`/`kmp-feature-scaffold`/`kmp-code-quality`/`kmp-library-publishing` (KI-008). |
| 2026-08-01 | Added an explicit non-KMP scope guard to "When to Use This Skill" — verified no literal trigger-keyword collision exists with unrelated stacks (React, Flutter, etc.), but nothing stated the boundary explicitly for a request naming a different stack outright or no stack at all. Now states plainly: don't route to this collection unless there's real Kotlin/KMP signal, even when the task shape sounds generic (a UI component, a state machine). |
| 2026-07-31 | Fixed the "Project-Specific Commands/Agents/Skills" canonical layout diagram — it never mentioned `.agents/skills/` at all, only `.claude/`, despite `.agents/skills/` being the actual cross-client deploy target this collection has used since an earlier fix. Added it to the diagram, and clarified project-root `skills/` is for custom skills only. Matches the same fix applied to `docs/reference/ai-collaboration.md`. |
| 2026-07-31 | Added `kmp-native-authoring` (66th skill) — real gap: `kmp-jni-pro` explicitly assumes the native C/C++ code already exists (its whole framing is "3rd-party files are read-only," library-first discovery) and never covered authoring brand-new first-party native source. Scaffolds directory layout, CMake, public C-ABI header design, and native-side testing; always hands off to `jni-pro` for the actual bridge. Added to the Meta list, Skill Invocation Map, and dependency graph. |
| 2026-07-31 | Added `kmp-api-mimicry` (65th skill) — a real gap: nothing covered mimicking a reference API's *shape* (Modifier-style chains, slot lambdas, DSL markers) when building a KMP library on a non-standard runtime (custom native renderer, custom transport) that isn't real Compose Multiplatform underneath. Distinct from `design-system`, which builds atop the real Compose runtime. Added to the Meta list, Skill Invocation Map, and dependency graph. |
| 2026-07-15 | Expanded the project-owned scaffold contract for Claude consumers: `rules/` and `docs/reference/ai-collaboration.md` are now part of the canonical source layout, and `CLAUDE.md` is explicitly treated as a thin bootstrap rather than the only copy of project policy. This keeps project-specific agent guidance at the repo root while `.claude/` remains the deployed runtime layer. |
| 2026-07-14 | Added "Project-Specific Commands/Agents/Skills — Source of Truth": a real gap found while reviewing a consumer project (a KMP game engine) whose two custom agent definitions were authored directly into `.claude/agents/` with no project-owned source anywhere. Documents mirroring this repo's own layout (`agents/`, `commands/`, `skills/`, `hooks/` at the project root as canonical source, `.claude/` as the deployed copy) for any project-specific artifact that isn't from `kmp-agent-skills` itself. Cross-referenced from `/kmp-setup-agents`, which only deploys this collection's own skills/commands, not project-owned ones. Corrected same-day: the layout initially nested a skill under an app-name folder (`skills/<app-name>/<skill-name>/`) — verified against `anthropic-skills:skill-creator`'s real, official skill anatomy that this isn't a recognized convention; skills are flat, named after what they do. Fixed to `skills/<skill-name>/`, with name-collision guidance (rename the skill, don't nest it) instead. |
| 2026-07-11 | Added an invocation-map row routing "composition over inheritance"/"abstract class in commonMain"/"agent over-abstracting" to `kmp-clean-architecture`'s new Composition Over Inheritance section — a real, recurring anti-pattern where an agent creates a public abstract class in commonMain requiring consumer inheritance. |
| 2026-07-11 | Added `kmp-docs-site` (62nd skill) — public GitHub Pages developer guide for a published library (MkDocs Material + Dokka HTML + compiler-verified snippet extraction), explicitly gated to library projects with real surface area, never apps or trivial libraries. Added to the Meta list and Skill Invocation Map. |
| 2026-07-10 | Two real gaps closed: (1) added a "Improve the performance of X" decision tree — there was no routing path for performance requests at all (only a model-routing hint, not a skill-routing rule); routes by naming what X is and explicitly stops rather than guessing when X is unnamed or whole-app; added `kmp-benchmark` (61st skill) as its "get a real number" branch. (2) Broadened "Which transport for a backend call?" to check for an existing Ktor client by content (`HttpClient(`/`safeRequest`/`NetworkResult<`) before the kRPC-specific grep — the prior version only checked kRPC symbols, so a project with a plain (differently-named) Ktor client and no kRPC could still fall through to a raw HTTP call; cross-referenced to `kmp-network-layer`'s new Step 0. |
| 2026-06-24 | Refined routing precedence for repo docs, downstream docs, changelogs, and navigation/deep-link collisions. |
| 2026-06-24 | Added architecture-diagram / library-docs / app-docs routing keywords for `kmp-project-docs-maintainer`. |
| 2026-06-24 | Added explicit release routing keywords (`release project`, `cut release`, `ship version`) so project release requests route to `kmp-release`. |
| 2026-06-24 | Added web routing / browser fragment / hash navigation keywords for `kmp-navigation`. |
| 2026-06-24 | Added direct designer routing for wireframes, screen flows, layout specs, and design handoff requests. |
| 2026-06-22 | Fixed kmp-jni-pro routing: was tagged with Kotlin/Native cinterop vocabulary (`CPointer`, `kotlin native interop`) but the skill is strictly JVM JNI. Corrected description + trigger map to JNI terms (`JNIEnv`, `Java_*`, `wrapper.cpp`, `vendor C++`, `C-shim`); added expect-actual disambiguation row for cinterop. |
| 2026-06-21 | Removed private project reference from docs-first rule; rule is now generic. |
| 2026-06-18 | Initial release — 30-skill routing map, dependency graph, build order, decision trees, anti-pattern checklist. |
